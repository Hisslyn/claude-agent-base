---
name: agents-list
description: List active, locked, and disabled agents in the roster.
allowed-tools: Bash(bash:*)
---
Run `bash "$CLAUDE_PROJECT_DIR/scripts/roster.sh" list` and report its output verbatim. Do nothing else.
