| id | file | issue | severity | found-by | status |
| --- | --- | --- | --- | --- | --- |
| QA-001 | scripts/tune-roster.sh | `git commit -- "$file" -m MSG` puts `-m`/message after the `--` pathspec separator, so git treats them as pathspecs and the commit fails; `\|\| true` swallows it, so tuned files are never committed | high | mechanical | fixed |
| QA-002 | scripts/tune-roster.sh | `grep -P` in cmd_status/cmd_finish is unsupported by BSD grep (present on host); status/finish counts silently break | high | mechanical | fixed |
| QA-003 | scripts/roster.sh | `find -type f` skips symlinked agent files, so list and frontmatter-name resolution hide them although stem resolution (`test -f`) follows symlinks | medium | mechanical | fixed |
| QA-004 | scripts/tune-roster.sh | `tunable_files` `find -type f` skips symlinked agents (same class as QA-003), excluding them from a tune pass | medium | mechanical | fixed |
| QA-005 | scripts/roster.sh | shfmt -i 2 formatting nonconformance | low | mechanical | fixed |
| QA-006 | scripts/tune-roster.sh | shfmt -i 2 formatting nonconformance | low | mechanical | fixed |
| QA-007 | scripts/new-agent.sh | shfmt -i 2 formatting nonconformance | low | mechanical | fixed |
| QA-008 | README.md / install model | Source dirs agents/, skills/, commands/ are not Claude Code discovery paths; per README they must be dropped into .claude/ or ~/.claude/ then the session restarted. The restarted session loaded only the user-level roster, so _probe-* were absent (Agent tool: "agent type not found"). Blocks live verification of suppression (1), skills preload (2), recursive discovery (3), and /agents-list (6). | high | currency | open |
| QA-009 | .claude/settings.json | Hook command hardcodes an absolute path instead of the snippet's $CLAUDE_PROJECT_DIR/scripts/validate-frontmatter.py; $CLAUDE_PROJECT_DIR is never exercised and the config is non-portable. Fix: use the hooks/settings.snippet.json form. | medium | currency | fixed |
| QA-010 | scripts/validate-frontmatter.py | is_agent_md() gates only on the ".claude/agents" substring, so edits to agent files in the source tree (agents/) exit 0 and are never validated; the badfm edit was not blocked. Confirmed: identical content under a .claude/agents path returns "missing or empty name", exit 2. Fix: broaden the gate to also match the source agents/ dir, or document that the hook guards only installed copies. | medium | currency | fixed |
| QA-011 | commands/agents-list.md (and siblings) | No allowed-tools frontmatter, so once installed the command's bash scripts/roster.sh call prompts for permission rather than running unattended. Fix: add allowed-tools: Bash(bash scripts/roster.sh:*) and analogous entries for the other command files. | low | currency | fixed |
| QA-012 | scripts/new-agent.sh | Kebab-case validator rejects names with a leading underscore, so /agent-new _t3 fails with "name must be kebab-case" — incompatible with the repo's own throwaway/test naming convention (_probe-*, _t*). Fix: allow a leading-underscore prefix for throwaways, or document that scaffolded names must be pure kebab. | low | integration | fixed |
| QA-013 | scripts/new-agent.sh + .claude/settings.json | /agent-new writes the scaffold via shell redirection (cat > file) inside a Bash call, not a Claude Write/Edit tool call; the PostToolUse matcher is Edit|Write|MultiEdit, so validate-frontmatter never fires at scaffold time — the exact moment frontmatter validation is wanted. (Scaffold itself is valid: validator exits 0 under a .claude/agents path.) Fix: add a Stop/SubagentStop or explicit post-scaffold validation, or have the command scaffold via the Write tool. | medium | integration | fixed |
| QA-014 | whole model (description-as-router-input) | PREMISE FAILURE: routing is flat description-matching — every agent's description is injected as router match text and folders are cosmetic (identity is frontmatter name). At 37 it works; at 100+ the router context bloats by thousands of tokens every turn and discrimination between overlapping "Use when..." descriptions collapses, so mis-routing becomes the norm. There is no routing tier. Min fix: mandate a two-level dispatch — domain coordinators with scoped agents: allowlists (the base already has orchestrator + agents:) so only a handful of descriptions compete at each hop; stop treating the flat roster as the routing surface. | high | design | fixed (documented as required pattern in agent-roster-reference) |
| QA-015 | scripts/validate-frontmatter.py + scripts/roster.sh | No name-uniqueness enforcement anywhere. The skill documents that a duplicate name makes Claude silently discard one file, yet the hook only checks name non-empty and list never flags dups. At scale across subfolders/teams a collision silently removes an agent from Claude's roster with zero signal, and resolve_file returns the FIRST find match so lock/disable/tune operate on an arbitrary one of the colliding files. Min fix: hook (or a list --check) that fails on any duplicate frontmatter name across the tree; resolve_file must collect all matches and error on >1. | high | design | fixed |
| QA-016 | commands/*.md (relative scripts/ path) | Every command calls bash scripts/roster.sh — a path relative to CWD. Installed to .claude/commands it only works when CWD is the project root; installed globally to ~/.claude/commands it runs against whatever project is current and breaks wherever scripts/ is absent. Decision: $CLAUDE_PROJECT_DIR is the correct anchor ONLY for project-vendored tooling (commands in .claude/commands + scripts in the project, referenced as "$CLAUDE_PROJECT_DIR/scripts/x.sh"); a GLOBAL command surface cannot use it (no guarantee the current project even contains the scripts) and must ship scripts to a fixed install location (e.g. ~/.claude/agent-tools/) referenced by an absolute/$HOME path resolved at install time. The bare relative path is correct for neither. Min fix: pick one install model and bake the matching anchor into the command bodies. | high | design | fixed (project-vendored: $CLAUDE_PROJECT_DIR) |
| QA-017 | scripts/tune-roster.sh | Manifest concurrency race. cmd_next greps the first pending line with no lock or claim, so two concurrent workers (an orchestration layer running parallel tunes) get handed the SAME file, both edit and both run done -> duplicate/conflicting edits and double commits. Concurrent done calls also race git index.lock and the commit fails (still swallowed). Min fix: flock the next+claim so it atomically transitions pending->inprogress and hands each file to exactly one worker; serialize the git commit (or commit outside the parallel region). | high | design | fixed |
| QA-018 | scripts/roster.sh + tune-roster.sh + new-agent.sh | Single-AGENTS_DIR assumption vs Claude's two-tier merge. agents_dir() resolves to EITHER project .claude/agents OR global ~/.claude/agents, never both, but Claude discovers the union (project shadows global by name). So list shows a different set than Claude actually routes, and lock/disable on a global agent is a no-op when a project agent shadows its name. Min fix: enumerate both scopes, render scope+shadowing in list, and make mutating ops refuse or warn when the target name is shadowed in the other scope. | high | design | fixed |
| QA-019 | scripts/roster.sh (lock) + commands/agent-lock.md | locked_ contradiction. list presents a LOCKED section implying out-of-rotation, but the skill confirms locked_ files are still .md with an unchanged name, so Claude keeps routing to them; "locked" is invisible to the platform and to any LLM actor unless it chooses to honor a filename prefix. Edit-protection encoded as an LLM-interpreted prefix is exactly the prose-guard anti-pattern the skill itself rejects ("enforced, unlike instructions"), and the agent-manager (Write/Edit) has nothing actually stopping it from editing a locked file. At scale with many actors the marker WILL be violated. Min fix: drop the prefix as a safety mechanism in favor of enforced protection (chmod 0444 / permission deny rules / move out of the discovery dir), or relabel it in list as advisory-only and never imply rotation state. | medium | design | fixed |
| QA-020 | scripts/roster.sh (disable/lock) | Rename ops create dangling/path references with no inbound check. Disabling a name that an orchestrator pins in its agents: allowlist makes that step unroutable (agents: overrides disable-model-invocation but not a vanished .off file); path-based hooks/settings/orchestrator configs that reference the file break on the locked_/.off rename; and tune-roster's manifest stores ABSOLUTE paths, so a lock/disable mid-pass leaves a stale path and done silently skips the commit ([[ -f "$file" ]] false). Min fix: before disable/lock, grep the tree (and known config) for inbound name/path references and refuse-or-warn; have the manifest key by name and re-resolve the path at done time. | medium | design | fixed |
| QA-021 | scripts/new-agent.sh | Scaffold is immediately auto-routable with garbage match text. The template ships model: sonnet, no disable-model-invocation, and a literal placeholder description ("...When to use it; what triggers auto-delegation..."); the refine step is a SEPARATE agent-manager call that can be skipped or fail. Every half-finished scaffold then competes in the router with meaningless text. Min fix: scaffold with disable-model-invocation: true by default so an unrefined agent cannot auto-route until a real description is written and the field removed. | medium | design | fixed |
| QA-022 | scripts/roster.sh + tune-roster.sh vs validate-frontmatter.py | Two frontmatter parsers that can disagree. roster/tune use an awk fm_field hack; the hook uses PyYAML. On valid-but-nontrivial YAML (quoted values like name: "foo", folded scalars, comments) awk returns the raw token (e.g. with quotes) while Claude/PyYAML parse the real value, so resolve_file and the manifest key on a string that does not match Claude's name. Min fix: one shared parser (call a tiny python frontmatter reader from the shell, or require PyYAML) used by every tool. | medium | design | fixed |
| QA-023 | scripts/tune-roster.sh | One global manifest per agents dir means only a single standard/pass can be in flight; init errors on a different standard. Even without concurrency this blocks running distinct per-subset or per-team passes, which is the natural need as the roster grows. Min fix: namespace the manifest by standard-hash (or accept a pass-id) so multiple independent passes coexist. | medium | design | fixed |
| QA-024 | scripts/validate-frontmatter.py | The single safety net validates only that name is non-empty. It does not check uniqueness (QA-015), description presence (an empty/placeholder description is the routing failure mode), tool-name validity, or model validity — so the things that actually break routing at scale pass the gate. Min fix: extend the hook to validate description non-empty/non-placeholder, known tool names, and (tree-wide) name uniqueness. | medium | design | fixed |
| QA-025 | scripts/roster.sh | O(n) full-scan design: list runs find 3x and spawns 2 awk processes per file; resolve_file scans and awk-parses files until a match. At 100+ agents that is hundreds of process spawns per command. Functional but a steady tax and a latency cliff. Min fix: build a name->path,model index once per invocation (single pass) and reuse it. | low | design | fixed |
| QA-026 | scripts/tune-roster.sh | Manifest (_roster_manifest) and .bak backups are written INTO the agents dir; when AGENTS_DIR is the global ~/.claude/agents they litter the user's global config dir, and they rely solely on a non-.md extension to stay out of discovery. Min fix: write transient state under a dedicated state dir (XDG/cache or .git) outside the discovery tree. | low | design | fixed |
